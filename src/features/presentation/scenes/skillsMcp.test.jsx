import React from 'react'; import { render, screen } from '@testing-library/react'; import { expect, it } from 'vitest'; import { SkillsScene } from './SkillsScene.jsx'; import { McpScene } from './McpScene.jsx'; void React; void SkillsScene; void McpScene; it('distinguishes skills plugins and MCP capabilities', () => { render(<><SkillsScene scene={{ thesis: 'Skills' }} /><McpScene scene={{ thesis: 'MCP' }} /></>); expect(screen.getByText('Skill ≠ plugin')).toBeInTheDocument(); expect(screen.getByText('MCP ≠ agent')).toBeInTheDocument(); });
