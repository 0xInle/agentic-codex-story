import React from 'react'; import { render, screen } from '@testing-library/react'; import { expect, it } from 'vitest'; import { PlanningScene } from './PlanningScene.jsx'; import { AgentsMdScene } from './AgentsMdScene.jsx'; void React; void PlanningScene; void AgentsMdScene; it('distinguishes spec and plan plus instruction scope', () => { render(<><PlanningScene scene={{ thesis: 'Plan' }} /><AgentsMdScene scene={{ thesis: 'Rules' }} /></>); expect(screen.getByText('SPEC задаёт успех; plan задаёт шаги.')).toBeInTheDocument(); expect(screen.getByText('Nested rules override root rules.')).toBeInTheDocument(); });
