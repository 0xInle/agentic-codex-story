import React from 'react';
import { appPath } from '../../../app/baseUrl.js';
void React;
export function HeroScene({ scene, landing = false }) { const thesis = scene.thesis || scene.screenThesis; const goals = scene.heroGoals || scene.hoverItems.map((item) => item.label); return <section className={landing ? 'hero-command-center landing-hero' : 'hero-command-center'} aria-label="Hero"><div className="hero-copy"><p className="eyebrow">AGENTIC DEVELOPMENT / 01</p>{landing ? <h1 className="hero-thesis">{thesis}</h1> : <p className="hero-thesis">{thesis}</p>}{!landing && scene.misconception ? <p className="hero-limit">{scene.misconception}</p> : null}{landing ? <div className="hero-actions"><a className="explore-primary-cta" href={appPath(`/present/${scene.id}`)}>Начать презентацию</a></div> : null}</div><ol className="hero-process-card" aria-label="Цели презентации">{goals.map((goal, index) => <li key={goal}><span>0{index + 1}</span>{goal}</li>)}</ol></section>; }
