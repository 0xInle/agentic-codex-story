import React from 'react'; import { fireEvent, render, screen } from '@testing-library/react'; import { expect, it } from 'vitest'; import { SpecificationScene } from './SpecificationScene.jsx'; import { ArchitectureScene } from './ArchitectureScene.jsx'; void React; void SpecificationScene; void ArchitectureScene;
it('advances and resets the deterministic spec builder', () => { render(<SpecificationScene scene={{ thesis: 'Spec' }} />); fireEvent.click(screen.getByRole('button', { name: 'Следующий шаг спецификации' })); expect(screen.getByText('Критерий успеха')).toBeInTheDocument(); fireEvent.click(screen.getByRole('button', { name: 'Сбросить спецификацию' })); expect(screen.getByText('Идея')).toBeInTheDocument(); });
it('compares architecture choices and shows the selected project structure', () => { render(<ArchitectureScene scene={{ thesis: 'Architecture' }} />); expect(screen.getByText('Feature-based')).toBeInTheDocument(); expect(screen.getByLabelText('Структура проекта')).toBeInTheDocument(); });
