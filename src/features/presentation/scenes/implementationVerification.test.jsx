import React from 'react'; import {render,screen} from '@testing-library/react'; import {expect,it} from 'vitest'; import {ImplementationScene} from './ImplementationScene.jsx'; import {VerificationScene} from './VerificationScene.jsx'; void React; void ImplementationScene; void VerificationScene; it('shows implementation and verification models',()=>{render(<><ImplementationScene scene={{thesis:'I'}}/><VerificationScene scene={{thesis:'V'}}/></>);expect(screen.getByText('Read → Change → Run → Report')).toBeInTheDocument();expect(screen.getByText('Build is not verification.')).toBeInTheDocument();});
