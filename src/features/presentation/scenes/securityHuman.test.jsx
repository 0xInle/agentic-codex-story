import React from 'react'; import {render,screen} from '@testing-library/react'; import {expect,it} from 'vitest'; import {SecurityScene} from './SecurityScene.jsx'; import {HumanDecisionScene} from './HumanDecisionScene.jsx'; void React; void SecurityScene; void HumanDecisionScene; it('renders least privilege and human responsibility',()=>{render(<><SecurityScene scene={{thesis:'S'}}/><HumanDecisionScene scene={{thesis:'H'}}/></>);expect(screen.getByText('Least privilege')).toBeInTheDocument();expect(screen.getByText(/Agentic development is not/)).toBeInTheDocument();});
