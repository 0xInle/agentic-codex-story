import React from 'react'; import { render, screen } from '@testing-library/react'; import { expect,it } from 'vitest'; import { OrchestrationScene } from './OrchestrationScene.jsx'; void React; void OrchestrationScene; it('explains orchestration without an interactive simulation',()=>{render(<OrchestrationScene scene={{thesis:'Orchestration'}}/>);expect(screen.getByText('Human checkpoint')).toBeInTheDocument();expect(screen.queryByRole('button')).not.toBeInTheDocument();});
