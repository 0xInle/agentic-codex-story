import React from 'react'; import { render, screen } from '@testing-library/react'; import { expect, it } from 'vitest'; import { AgentsScene } from './AgentsScene.jsx'; void React; void AgentsScene; it('renders delegation fields and fit boundaries', () => { render(<AgentsScene scene={{ thesis: 'Agents' }} />); expect(screen.getByText('Role')).toBeInTheDocument(); expect(screen.getByText('Shared files: do not delegate in parallel.')).toBeInTheDocument(); });
