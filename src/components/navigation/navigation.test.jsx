import { expect, it } from 'vitest'; import { render, screen } from '@testing-library/react'; import { createElement } from 'react'; import { SkipLink } from './index.js'; it('keeps a direct skip link for the focused landing and slides', () => { render(createElement(SkipLink)); expect(screen.getByRole('link', { name: 'К основному содержимому' })).toHaveAttribute('href', '#main-content'); });
