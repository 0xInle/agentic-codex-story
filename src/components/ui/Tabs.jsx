import React from 'react';
void React;
export function Tabs({ tabs, activeId, onChange }) { const onKeyDown = (event, index) => { if (!['ArrowLeft', 'ArrowRight'].includes(event.key)) return; event.preventDefault(); onChange(tabs[(index + (event.key === 'ArrowRight' ? 1 : tabs.length - 1)) % tabs.length].id); }; return <><div role="tablist" aria-label="Разделы">{tabs.map((tab, index) => <button key={tab.id} id={`tab-${tab.id}`} role="tab" aria-controls={`panel-${tab.id}`} aria-selected={tab.id === activeId} tabIndex={tab.id === activeId ? 0 : -1} onClick={() => onChange(tab.id)} onKeyDown={(event) => onKeyDown(event, index)}>{tab.label}</button>)}</div>{tabs.map((tab) => <div key={tab.id} id={`panel-${tab.id}`} role="tabpanel" aria-labelledby={`tab-${tab.id}`} hidden={tab.id !== activeId}>{tab.panel}</div>)}</>; }
