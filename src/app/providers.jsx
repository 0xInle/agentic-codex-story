import React, { createContext, useReducer } from 'react'; import { appReducer } from '../state/appReducer.js'; import { initialState } from '../state/initialState.js'; void React; export const AppContext = createContext(null); export function Providers({ children }) { const [state, dispatch] = useReducer(appReducer, initialState); return <AppContext.Provider value={{ state, dispatch }}>{children}</AppContext.Provider>; }
