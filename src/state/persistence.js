export const PERSISTENCE_VERSION = 1; export function parsePersistedState(value) { try { const parsed = JSON.parse(value); return parsed?.version === PERSISTENCE_VERSION ? parsed : null; } catch { return null; } } export function serializePersistedState(state) { return JSON.stringify({ version: PERSISTENCE_VERSION, lastSceneId: state.lastSceneId, reducedMotion: Boolean(state.reducedMotion), keyboardHelpDismissed: Boolean(state.keyboardHelpDismissed), navigationOrigin: state.navigationOrigin ?? null }); }
