import { scenes } from '../content/scenes/scenes.js'; export function appReducer(state, action) { const index = scenes.findIndex((scene) => scene.id === state.activeSceneId); if (action.type === 'NEXT_SCENE' || action.type === 'PREVIOUS_SCENE') { const next = scenes[Math.max(0, Math.min(scenes.length - 1, index + (action.type === 'NEXT_SCENE' ? 1 : -1)))].id; return { ...state, activeSceneId: next, visitedSceneIds: [...new Set([...state.visitedSceneIds, next])] }; } if (action.type === 'GO_TO_SCENE' && scenes.some((scene) => scene.id === action.sceneId)) return { ...state, activeSceneId: action.sceneId, visitedSceneIds: [...new Set([...state.visitedSceneIds, action.sceneId])] }; if (action.type === 'SET_MODE') return { ...state, mode: action.mode }; if (action.type === 'START_TIMER' || action.type === 'PAUSE_TIMER') return { ...state, presentation: { ...state.presentation, running: action.type === 'START_TIMER' } }; if (action.type === 'RESET_TIMER') return { ...state, presentation: { elapsedMs: 0, running: false } }; return state; }
