# Правила модуля настроек профиля

Работайте только в границах `profile-settings`, если задача не расширена явно.
Сначала прочитайте `README.md`, `architecture.md` и `verification.md`, затем
выберите нужную ветку: `api`, `model` или `ui`.
