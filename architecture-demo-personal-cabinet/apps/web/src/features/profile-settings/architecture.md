# Архитектура фичи

Страница вызывает фичу. UI передаёт намерение пользователя в модель, модель
координирует состояние, а API-клиент сохраняет настройку по контракту. UI не
должен знать детали HTTP-запроса.
