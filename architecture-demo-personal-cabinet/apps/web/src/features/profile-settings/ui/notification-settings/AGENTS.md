# Правила UI настроек уведомлений

Перед изменением читайте `component-contract.md` и `accessibility.md`. UI
отображает состояние модели и передаёт событие изменения наверх; он не создаёт
собственный API-запрос и не обходит модель.
