# Контракт переключателя уведомлений

`checked` показывает текущее значение настройки. `disabled` блокирует действие
во время загрузки или сохранения. `onChange` сообщает о намерении пользователя;
сохранение и обработка ошибки остаются за моделью фичи.
