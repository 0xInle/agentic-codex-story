# Правила компонента NotificationSwitch

Работайте только с контрактом компонента и его соседним тестом. До изменения
прочитайте два уровня выше `component-contract.md` и `accessibility.md`; не
переносите в компонент логику API или управления состоянием фичи.
