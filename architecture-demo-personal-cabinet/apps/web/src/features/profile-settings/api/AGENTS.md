# Правила API-слоя

Перед изменением читайте `profile-contract.md`. Не меняйте форму запроса или
ответа без одновременного обновления контракта и согласования с владельцем API.
API-слой возвращает данные и ошибки модели, а не управляет UI.
