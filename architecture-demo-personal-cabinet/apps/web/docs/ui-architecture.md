# Архитектура интерфейса

`pages` владеют маршрутом и собирают экран. `features` владеют бизнес-поведением
пользовательской возможности. `shared` не знает о профиле и предоставляет
переиспользуемые кнопки, HTTP-утилиты и базовые элементы интерфейса.
